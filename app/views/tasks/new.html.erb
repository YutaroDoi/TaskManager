<% provide(:title, 'Create Tasks') %>
<% provide(:button_text, '作成') %>
<% provide(:request_path, tasks_path) %>
<h1>タスク作成</h1>

<div class="container">
<div class="row">
  <div class="col-md-6 offset-md-3">
    <%= render 'form' %>
  </div>
  <div class="col-3">
    <%= render 'shared/label_form'%>
  </div>
</div>
</div>
